# Debug Repetition Test Configuration (sweep_test format)
# Run with: python -m vllm_exp.run sweep-test --config vllm_exp/configs/debug_repetition_sweep.yaml --log-dir logs/agent/

project: "resize-test-full"
type: "sweep_test"

envs:
  RAY_DEDUP_LOGS: "0"  # Disable Ray log deduplication for debugging
  NCCL_DEBUG: INFO
  NCCL_CUMEM_HOST_ENABLE: "0"
  VLLM_NCCL_SO_PATH: "/apps/easybuild-2022/easybuild/software/Compiler/GCCcore/11.3.0/NCCL/2.21.5-CUDA-12.4.1/lib/libnccl.so.2.21.5"
  VLLM_TORCH_PROFILER_DIR: /home/bxb1/data/vllm_workbench/profiler/
  RAY_CGRAPH_get_timeout: "10000"
  BENCHMARK_CONFIG_PATH: "/home/bxb1/data/tmp/vllm_exp/benchmark_config.json"
  DEPLOYMENT_CONFIG_PATH: "/home/bxb1/data/tmp/vllm_exp/vllm_config.json"
  KV_SYNC_USE_CPU: "0"
  VLLM_USE_CPU_MODEL: "0"
  CUDA_LAUNCH_BLOCKING: "0"
  PYTORCH_CUDA_ALLOC_CONF: "max_split_size_mb:32"
  VLLM_USE_RAY_COMPILED_DAG_OVERLAP_COMM: "0"
  VLLM_USE_RAY_COMPILED_DAG: "1"
  NCCL_SOCKET_IFNAME: "bond0.3027"
  GLOO_SOCKET_IFNAME: "bond0.3027"
  VLLM_SERVER_DEV_MODE: "1"  # Enable /set_pp_config endpoint
  VLLM_DISABLE_CUTLASS_FP8: "1"  # Workaround: cutlass produces NaN on L40s
is_log_cover: true
static_config:
  model:
    path: "/data/gpfs/projects/punim2715/huggingface/Llama-3.3-70B-Instruct-FP8"
    name: "Llama-3.3-70B-Instruct-FP8"
  network:
    rank_to_ip:
      0: "spartan-gpgpu166"
      1: "spartan-gpgpu007"
    rank_to_node:
      0: "spartan-gpgpu166"
      1: "spartan-gpgpu007"
  vllm:
    head_addr: "localhost"
    pipeline_parallel_size: 2
    gpu_memory_utilization: 0.95
    max_model_len: 4000
    chunked_prefill: true
    enable_cuda_graph: false
    enable_nsight: false
    log_kv_memory_stats: true
    weight_chunk_size_mb: 5
    weight_loading_mode: sync
    disable_memory_overhead_monitor: false
    max_num_batched_tokens: 4064
  benchmark:
    restart_server_between_repetitions: true
    burstiness: 100.0
    benchmark_script_path: "/home/bxb1/vllm_workbench/vllm/benchmarks/benchmark_serving.py"
    profile: false
    num_total_requests: 100  # Shared across all sweep combinations
    print_outputs: true
    warmup:
      enabled: false
      num_total_requests: 25
      input_length: 10000
      output_length: 50
      request_rate: 25
sweep_configs:
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      weight_loading_mode: [async]
      enable_kv_resize: [false, true]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
      # block_size: [128, 256] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5         # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "56, 24"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 1.5
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 1.5
            input_lens: 128
            output_lens: 512
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      weight_loading_mode: [async]
      enable_kv_resize: [false, true]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5         # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "52, 28"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 3
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 3
            input_lens: 128
            output_lens: 512
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      weight_loading_mode: [async]
      enable_kv_resize: [false, true]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
      # block_size: [128, 256] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5         # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "56, 24"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 2 
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 2 
            input_lens: 128
            output_lens: 512
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      weight_loading_mode: [async]
      enable_kv_resize: [false, true]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
      # block_size: [128, 256] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5         # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "52, 28"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 2.5
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 2.5
            input_lens: 128
            output_lens: 512
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      weight_loading_mode: [async]
      enable_kv_resize: [false, true]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
      # block_size: [128, 256] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5         # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "56, 24"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 0.5 
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 0.5 
            input_lens: 128
            output_lens: 512
  - vllm:
      attention_kernel: [direct]
      fixed_num_gpu_blocks: [-1] 
      weight_chunk_size_mb: [160]
      migration_approach: [async_fast]
      enable_kv_resize: [false, true]
      weight_loading_mode: [async]
      use_vmm: [true]
      block_size: [128] # Granularity equals to 2
      # block_size: [128, 256] # Granularity equals to 2
    benchmark_config:
      - num_total_requests: 200  # Small number for quick testing
        repetition: 5        # 3 repetitions to test the feature
        pp_layer_config:
          0: "36, 44"           # Single config = no migration, only reset between reps
          100: "52, 28"           # Single config = no migration, only reset between reps
        requests:
          0:
            request_rate: 1 
            input_lens: 512
            output_lens: 16
          100:
            request_rate: 1 
            input_lens: 128
            output_lens: 512